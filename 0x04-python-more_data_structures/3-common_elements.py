#!/usr/bin/python3
def common_elements(set_1, set_2):
    common_element = set_1.intersection(set_2)
    return common_element

    # return set_1 and set_2 other form

    ''' 
    Intersection of two list means we need to
    take all those elements which are common to
    both of the initial lists and store them into
    another list.
    '''
